# 1315 : Too many arguments

The parser detected a function or an initialization of an object with too many arguments.

&#x20;

The following example illustrates this error:

```
method main(arg) // error 1315: 'main' : too many arguments
{
variable v;
v = 1;
} 
```

